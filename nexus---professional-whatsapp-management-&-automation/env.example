# Optional admin protection
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
ADMIN_TOKEN_SECRET=replace-with-long-random-secret

# Runtime port (AWS ECS/App Runner/Beanstalk usually inject this)
PORT=3000
